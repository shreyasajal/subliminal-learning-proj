# P2 anchor replication. Nief's exact reported config, one seed, 7B.
# This is the config that must reproduce a published number before anything
# else in the project is meaningful.
extends: lora.yaml
model: qwen7b
rank: 8
optimizer: adamw
trait: cat
seed: 0
expected: |
  GATE: trained cat student at r=8, eval context qwen ~= 39%.
  Published targets across the three eval contexts:
      qwen (matched) ~39%    empty ~2.6%    chatgpt ~1.0%
  Reported delta = qwen - empty.
  Control-student and untrained-baseline should sit near the base rate in ALL
  three contexts. If the control arm is elevated, the filter leaked and P1
  must be redone.
